docker compose logs -f resolver_app

docker compose build
docker compose up --build -d
docker compose up
docker compose down
docker compose down -v

docker logs -f dns_resolver_NO_1
docker logs -f ns_mycloud
docker logs -f ns_rotld

docker logs -f ns_root

#ca sa aflii ip ul
docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' dns_resolver_NO_1

docker ps -a
docker stats
docker network ls